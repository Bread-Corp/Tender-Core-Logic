name: Deploy Lambda on Release Merge

on:
  push:
    branches:
      - release
  workflow_dispatch: # Allows manual trigger from GitHub Actions UI

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0'

      - name: Restore dependencies
        run: dotnet restore Tender-Core-Logic/Tender-Core-Logic.sln

      - name: Build
        run: dotnet build Tender-Core-Logic/Tender-Core-Logic.sln --configuration Release

      - name: Publish Lambda package
        run: dotnet publish Tender-Core-Logic/Tender-Core-Logic/src/Tender-Core-Logic/Tender-Core-Logic.csproj -c Release -o ./publish

      - name: Zip Lambda package
        run: zip -r lambda.zip ./publish/*

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Deploy to AWS Lambda
        run: |
          aws lambda update-function-code \
            --function-name ${{ secrets.LAMBDA_FUNCTION_NAME }} \
            --zip-file fileb://lambda.zip
